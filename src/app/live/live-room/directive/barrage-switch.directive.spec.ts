import { BarrageSwitchDirective } from './barrage-switch.directive';

describe('BarrageSwitchDirective', () => {
  it('should create an instance', () => {
    const directive = new BarrageSwitchDirective();
    expect(directive).toBeTruthy();
  });
});
