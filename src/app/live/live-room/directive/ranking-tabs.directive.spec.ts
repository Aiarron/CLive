import { RankingTabsDirective } from './ranking-tabs.directive';

describe('RankingTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new RankingTabsDirective();
    expect(directive).toBeTruthy();
  });
});
