import { ShowRankDirective } from './show-rank.directive';

describe('ShowRankDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowRankDirective();
    expect(directive).toBeTruthy();
  });
});
