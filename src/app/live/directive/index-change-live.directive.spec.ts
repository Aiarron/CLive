import { IndexChangeLiveDirective } from './index-change-live.directive';

describe('IndexChangeLiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IndexChangeLiveDirective();
    expect(directive).toBeTruthy();
  });
});
