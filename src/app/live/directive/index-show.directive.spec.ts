import { IndexShowDirective } from './index-show.directive';

describe('IndexShowDirective', () => {
  it('should create an instance', () => {
    const directive = new IndexShowDirective();
    expect(directive).toBeTruthy();
  });
});
