import { IndexGotoRoomShowDirective } from './index-goto-room-show.directive';

describe('IndexGotoRoomShowDirective', () => {
  it('should create an instance', () => {
    const directive = new IndexGotoRoomShowDirective();
    expect(directive).toBeTruthy();
  });
});
