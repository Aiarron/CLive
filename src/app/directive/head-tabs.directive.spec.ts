import { HeadTabsDirective } from './head-tabs.directive';

describe('HeadTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadTabsDirective();
    expect(directive).toBeTruthy();
  });
});
