import { IndexShowInfoDirective } from './index-show-info.directive';

describe('IndexShowInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new IndexShowInfoDirective();
    expect(directive).toBeTruthy();
  });
});
